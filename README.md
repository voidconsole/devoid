# voidnet
an encrypted custom virtual server hosted on raspberry pi
- uses wireguard to host a secure network tunnel
- requires Public IP and a NAT on home router
- requires a server (preferably linux) like rpi
- is end to end encrypted with ChaCha20
